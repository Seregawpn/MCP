## MBP‑1 — Браузерный ассистент (selector-based), TTS и PTT beta

### Цель и результат
- Пользователь даёт натуральную команду → ассистент выполняет последовательность шагов В БРАУЗЕРЕ, по одному инструменту за итерацию, до статуса `done`.
- **ОБНОВЛЕНО: Основной способ взаимодействия — через CSS селекторы и текстовый поиск (selector-based) вместо numeric-first.**
- **ОБНОВЛЕНО: Система автоматически анализирует страницу после каждого действия и предоставляет LLM детальную информацию.**
- Допускаются уточняющие вопросы (`assistant_ask`) при нехватке данных.
- Объём MVP‑1: Браузер + TTS (обязательно) + PTT (beta). MCP для Файлов/Приложений/Почты/iMessage — исключены из MVP‑1 и перенесены в последующие релизы.

### Объём функциональности (браузер + TTS/PTT beta)
- **ОБНОВЛЕНО: Оркестратор: observe → plan → act → validate → ask/done; 1 итерация = 1 function_call + пауза ~2с. После действий — автоматический анализ страницы + скриншот для LLM.**
- **ОБНОВЛЕНО: MCP (браузер): navigate, wait, browser_click_text, browser_type_text, browser_click_selector, browser_type_selector, browser_extract, screenshot.**
- **ОБНОВЛЕНО: Убраны numeric-first инструменты: list_interactives, overlay_show/hide, overlay_act.**
- MCP (озвучка): tts_speak(text, voice, rate), tts_stop().
- PTT beta: удержание клавиши → запись → ASR (Apple Speech/Whisper) → текст цели/ответа.
- **ОБНОВЛЕНО: SYSTEM PROMPT: строгий формат ответа (mode=act|ask|done), selector-based, детальный анализ страниц, ask/done.**

---

### Поэтапная реализация (микро‑проекты, только MVP‑1)

#### **МП‑1: Браузер (MCP) + Анализ страниц ✅ ВЫПОЛНЕНО**
- **Функции: navigate, browser_click_text, browser_type_text, browser_click_selector, browser_type_selector, browser_extract, screenshot, wait.**
- **ОБНОВЛЕНО: Автоматический анализ страницы после каждого действия:**
  - Детальный результат действия (что произошло, статус, детали)
  - Анализ изменений на странице (текст, прогресс)
  - Скриншоты для визуального контекста
- **ОБНОВЛЕНО: Убрана numeric-first логика, заменена на selector-based взаимодействие**
- **DoD: ✅ Выполнено: анализ страниц работает, циклы исправлены, детальные результаты предоставляются, скриншоты создаются.**

#### **МП‑2: TTS (Голосовое сопровождение) 🎯 СЛЕДУЮЩИЙ ШАГ**
- **Функция: tts_speak для озвучивания каждого шага**
- **Что нужно:**
  - Prompt для генерации речи на каждом шаге
  - TTS инструменты в MCP сервере
  - Интеграция озвучки в orchestrator
  - Озвучка: "Открываю сайт...", "Ищу элемент...", "Выполняю действие...", "Результат получен..."
- **DoD: Каждый шаг озвучивается для слепых пользователей, они понимают что происходит на каждом этапе.**

#### **МП‑3: LLM‑планировщик + цикл ask/done**
- Функция: planner_llm с жёсткой схемой; интеграция в оркестратор; 1 шаг = 1 tool‑call; пауза 2с; selector-based; фоллбеки; LLM‑summary.
- DoD: 3 сценария от натуральной команды; ≤2 уточнения; done=true только при верификации результата; логи planner_prompt/response. В LLM‑summary включены title/snippet и анализ страницы.

#### **МП‑4: PTT beta (опционально в MVP‑1)**
  - Удержание клавиши → запись 16 kHz WAV → ASR → текст цели/ответа; логи ptt_start/stop/asr_result.
  - DoD: стабильный старт/стоп; корректный ввод текста; озвучка подтверждает распознанное.

#### **МП‑5: Совместимость с VoiceOver и клавиатура**
- Хоткеи: Tab/Shift+Tab, Enter, Esc, Space, R, H, C, L, S, O, I, Cmd+.
  - DoD: чек‑лист VO проходит; паник‑стоп работает; хоткеи переназначаемые.

### **ОБНОВЛЕНО: Что уже сделано (браузер MCP) ✅**
- **Реализовано и работает:**
  - `browser_navigate` - навигация по URL
  - `browser_click_text` - клик по тексту элемента
  - `browser_type_text` - ввод текста в поле по названию
  - `browser_click_selector` - клик по CSS селектору
  - `browser_type_selector` - ввод текста по CSS селектору
  - `browser_extract` - извлечение текста страницы
  - `browser_wait` - ожидание загрузки
  - `browser_screenshot` - создание скриншотов
- **ОБНОВЛЕНО: Убраны numeric-first инструменты:**
  - ❌ `browser_list_interactives` - заменен на `browser_extract`
  - ❌ `browser_overlay_show/hide` - больше не нужны
  - ❌ `browser_overlay_act` - заменен на прямые селекторы
- **ОБНОВЛЕНО: Автоматический анализ страницы:**
  - После каждого действия система анализирует что изменилось
  - Предоставляет LLM детальную информацию о результате
  - Создает скриншоты для визуального контекста
  - Анализирует прогресс задачи и изменения на странице

### **ОБНОВЛЕНО: Что планируем (ближайшие 3–5 дней) 🎯**
- **СЛЕДУЮЩИЙ ШАГ: TTS (Голосовое сопровождение)**
  - Prompt для генерации речи на каждом шаге
  - TTS инструменты в MCP сервере
  - Интеграция озвучки в orchestrator
  - Тестирование с реальными пользователями
- **После TTS:**
  - LLM планировщик с ask/done циклом
  - PTT beta для голосового ввода
  - VoiceOver совместимость

### **ОБНОВЛЕНО: Чек‑лист запуска работ**
- [x] **✅ Исправлены циклы LLM** - система не застревает в бесконечных поисках
- [x] **✅ Добавлен анализ страниц** - LLM получает детальную информацию после каждого действия
- [x] **✅ Убрана numeric-first логика** - заменена на selector-based взаимодействие
- [x] **✅ Добавлены детальные результаты** - LLM понимает что именно произошло
- [x] **✅ Добавлены скриншоты** - визуальный контекст для LLM
- [ ] **🎯 СЛЕДУЮЩИЙ: TTS озвучка каждого шага** для слепых пользователей
- [ ] LLM планировщик с ask/done циклом
- [ ] PTT beta для голосового ввода
- [ ] VoiceOver совместимость

### **ОБНОВЛЕНО: План работ (ориентир 1 неделя)**
- **День 1-2: ✅ ВЫПОЛНЕНО** - Исправление циклов, анализ страниц, детальные результаты
- **День 3: 🎯 СЛЕДУЮЩИЙ** - TTS (голосовое сопровождение каждого шага)
- **День 4:** LLM планировщик + ask/done цикл
- **День 5:** PTT beta + VoiceOver совместимость
- **День 6-7:** Тестирование, полировка, финальные прогоны

### **ОБНОВЛЕНО: Уточнённые MCP‑эндпоинты (только MVP‑1)**
```json
{
  "browser.navigate": { "url": "string" },
  "browser.wait": { "ms": 2000 },
  "browser.click_text": { "text": "string" },
  "browser.type_text": { "field": "string", "text": "string" },
  "browser.click_selector": { "selector": "string" },
  "browser.type_selector": { "selector": "string", "text": "string" },
  "browser.extract": { "mode": "text|summary" },
  "browser.screenshot": { "full_page": false },

  "tts.speak": { "text": "string", "voice": "string", "rate": 1.0 },
  "tts.stop": {}
}
```

### **ОБНОВЛЕНО: Что уже работает в текущей системе ✅**
- **Браузерные инструменты:** Все основные действия через селекторы и текст
- **Анализ страниц:** Автоматический анализ после каждого действия
- **Детальные результаты:** LLM получает полную информацию о том что произошло
- **Скриншоты:** Визуальный контекст для LLM
- **Исправлены циклы:** Система не застревает в бесконечных поисках
- **Убрана numeric-first:** Больше нет проблем с нумерацией элементов

### **ОБНОВЛЕНО: Критические улучшения для слепых пользователей 🎯**
- **СЛЕДУЮЩИЙ ШАГ: TTS озвучка каждого шага**
  - Без озвучки слепые пользователи не понимают что происходит
  - Каждый шаг должен быть озвучен: "Открываю сайт...", "Ищу элемент...", "Выполняю действие..."
  - Озвучка прогресса, ошибок, завершения задачи
- **После TTS:**
  - Голосовое управление (PTT)
  - VoiceOver совместимость
  - Горячие клавиши для управления

### **ОБНОВЛЕНО: Архитектурные изменения ✅**
- **Убрана сложная numeric-first логика** - заменена на простые селекторы
- **Добавлен автоматический анализ страниц** - LLM всегда понимает что происходит
- **Упрощена структура инструментов** - меньше сложности, больше надежности
- **Добавлены детальные результаты** - полная информация для LLM
- **Интегрированы скриншоты** - визуальный контекст

### **ОБНОВЛЕНО: Система работает следующим образом 🔄**
1. **Пользователь дает команду** (например, "Найди хип-хоп на YouTube")
2. **LLM анализирует задачу** и выбирает инструмент
3. **Выполняется действие** (navigate, click, type)
4. **Автоматически анализируется страница:**
   - Детальный результат действия
   - Изменения на странице
   - Скриншот для визуального контекста
5. **LLM получает полную информацию** и планирует следующий шаг
6. **Процесс повторяется** до завершения задачи
7. **🎯 СЛЕДУЮЩИЙ: Каждый шаг озвучивается через TTS**

### **ОБНОВЛЕНО: Преимущества новой архитектуры ✅**
- **Надежность:** Нет проблем с нумерацией и циклами
- **Понятность:** LLM всегда понимает что происходит
- **Простота:** Меньше сложной логики, больше стабильности
- **Анализ:** Автоматический анализ страниц после каждого действия
- **Контекст:** Скриншоты + текст + результаты для полного понимания
- **🎯 Готовность к TTS:** Архитектура готова для добавления голосового сопровождения
